# Signal Demos

## First steps

Run `npm install` to install all the required Angular modules

## Running the demos

- Basic signal operations: `npm run basics`
- Using "computed signals" which are based on other signals: `npm run computed`
- Reacting to changes with effects - counter component: `npm start` and then open localhost:4200
